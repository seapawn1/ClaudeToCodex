SM/Codex 向 Sprint 04 Developer 发布 PO 已确认的推进路径。PO 已授权我向本会话发布任务并指导推进；需要 PO 本人参与时由我报告具体事项。请先完整阅读项目 CLAUDE.md、memory 索引及 main 工作区中刚更新的 scrum/sprint-04-multi-claude-sessions/SprintBacklog.md。

范围：先做 Sprint 04 / PBI-11，以已发布 1.0.0 为基线，独立于 Sprint 03。目标是同一个 Codex 原始会话与至少两个明确的 Claude 原始会话保持独立配对、交错交流，无人工搬运消息。03 的呈现改版和完整快捷连接管理体验留后续。

路径：1) 独立工作区中的短预研与原型；2) 专用测试 Codex + Claude A/B 的真实闭环；3) 原型成立后再整合开发工作区中的产品代码，并在独立测试安装里验收候选插件。优先评估多个独立配对加统一路由/收件入口，复用现有命名管道 priority=next 与 queue/hooks。此为评估方向，存储、结构、任务分解、顺序和估计由你决定。

硬边界：原型阶段不改产品源码与插件分发副本；全程不改日常已安装插件、缓存、marketplace、共享配置或在用桥数据。默认桥属于其他项目且有待收消息，禁止改配对或归档它。测试须隔离代码、配置、工作目录、桥数据与生效 hooks；原型目录放本 Sprint 目录内并使用独立 worktree，不能把本开发会话当作随意重启/停止的测试对端。宿主授权不得绕过。

首个检查点：两个配对共存，切到 B 后仍正确回复 A，接近同时来信的接纳/等待/拒绝有明确结果且不串目标、覆盖或重复注入；空闲收信触发下一次模型调用。之后补单目标失效隔离、单目标兼容、数据处理与安装候选验收。fixture、加载、执行、原会话收信分别判定；submitted 或 context-prepared 不单独证明收信。PO 不手写标记、ID、环境变量。原型通过不等于插件 DoD。

请先回报你对范围/隔离边界的理解、worktree 与文件归属、首批可执行工作项/估计及阻碍，再自主推进已授权的预研与原型；不要只回复收到。Developer 工作区由你填写，SM 维护主树的范围与 Product Backlog；避免整文件覆盖或改写 SM 区域。里程碑、范围风险或需要 PO 授权时及时报告。

协调说明：本消息使用独立 s04-coordination-01a09641 目录；SM 当前 hooks 仍服务既有默认配置，因此标准回桥的唤醒未必能交付本协调目录的正文。请将本轮实质回复写入你原始会话，并用 codex queue --thread 01a09641-0761-73c1-8a5e-e49692fee440 --message <完整工作回报> 回报 SM；此为临时原生协调通道，不是 Sprint 04 插件验收路径。勿更改 SM 的 hooks 或日常配对来修复协调通道。
