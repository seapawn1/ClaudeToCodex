# Sprint 04 Backlog：一 Codex 对多 Claude Code 会话

- 创建：2026-09-11；范围确认：2026-09-12；本次修订：2026-09-13。
- 范围：PO 已决定先推进 Sprint 04 / PBI-11；PBI-09、PBI-10、PBI-12、PBI-13 留在 Sprint 03。
- 状态：进入 Planning 与隔离原型准备；Developer 评估可行性、工作项与容量，技术细节由其确定，需要 PO 参与的事项由 SM 汇报。
- 参与者：PO、SM / Codex、Developer / Claude Code。PO 指定的 Developer 会话为 `ClaudeToCodex # [developer]-[sprint-04]-[开始执行sprint-04冲刺]`。
- 节奏：时长、开始日及收口日期待 Planning 明确。
- 基线：以已发布的 1.0.0 通信能力为基础，Sprint 04 独立推进与验收，不以前置完成 Sprint 03 为条件。

## 第一部分：Sprint Goal 与 DoD

### Sprint Goal（PO 方向，待团队 Planning 收敛）

> 让 PO 通过同一个 Codex 原始会话，与至少两个可明确辨认的 Claude Code 原始会话持续开展独立交流，无需为了更换交流对象而拆除另一条连接，且每次消息、回复与连接操作都作用于正确目标。

与 Product Goal 的关系：将两个原始会话直接交流的价值扩展到多个协作对象，使多个项目或同项目内的协作可以持续推进。最低可验收规模为一个 Codex、两个 Claude；多个配对共存不自动承诺任意规模、广播或并发吞吐。

### DoD（与 Product Backlog 一致）

**Definition of Outcome Done**

PO 在真实使用场景中亲身体验产品，确认当前 Product Goal 所约定的价值已经实现，并记录体验场景与结论。

**Definition of Output Done**

Increment 已集成到产品中，可通过标准产品入口使用，通过与其声明范围相适应的质量验证，并由 PO 按事先约定的验收标准验收通过。

## 第二部分：选入 PBI 与验收标准

来源：[Product Backlog](../ProductBacklog.md)；后续体验范围见 [Sprint 03 Backlog](../sprint-03-usability-polish/SprintBacklog.md)。PBI-11 已由 PO 确认为优先推进方向；Developer 对可完成范围的预测待研究与容量评估。

| 编号 | 标题 | 用户故事 | 当前状态 |
|---|---|---|---|
| PBI-11 | 一 Codex 对多 Claude Code 会话 | 作为日常使用者，我要让一个 Codex 会话同时与多个 Claude Code 会话保持桥接并按名称往来，以便并行开展多项协作，而不是每次换目标都要换配对。 | PO 已授权先推进；Developer 进行 Planning 与隔离原型评估 |

### 与 Sprint 03 的责任边界（无前置依赖）

| 能力 | Sprint 03 的范围 | Sprint 04 的新增要求 |
|---|---|---|
| 目标辨识（PBI-12） | 多个候选中认出并选对当前单一配对目标 | 多个已连接目标可持续区分；名称、项目与原会话身份关系稳定 |
| 消息呈现（PBI-09） | 可读正文、回复入口、正确唤醒与原会话接收 | 同一 Codex 中来自不同目标的往来可区分，回复不受最近选择影响 |
| 状态与换配对（PBI-10） | 单配对状态、沿用、归档及显式重建 | 每个目标有可辨认的状态与重建边界，不影响其他目标 |
| 连接操作（PBI-13） | 状态入口、显式断开、待收处置与重连 | 操作明确作用于单个目标，其他目标继续可用，旧消息不串入其他配对 |

Sprint 04 以 1.0.0 为基线，完成多配对所必需的目标辨识、基本状态、回复归属和单目标操作边界。Sprint 03 后续负责完整的呈现、目标辨识交互及快捷连接管理体验；这些体验不作为 Sprint 04 的前置依赖或验收条件。

### 推进与隔离边界

- 先在独立工作区编写原型代码，验证多配对、路由和统一收件；原型阶段保持产品源码、分发副本及已安装插件原状。具体架构和分解由 Developer 决定。
- 使用专用测试 Codex 与两个测试 Claude，隔离工作目录、配置、桥数据及生效 hooks。不得修改日常插件安装、缓存、marketplace、共享配置或在用桥；对话内禁用若可能影响共享配置，不作为默认隔离办法。
- 最小真实检查点覆盖两个配对共存、交错回复不串目标、相近时间来信处理。原型通过后再在开发工作区整合产品代码，并在独立测试安装中验收候选插件；日常安装更新由 PO 另行决定。
- Developer 与 SM 组织环境准备和技术验证；需要 PO 登录、宿主信任、范围取舍或集中体验时，由 SM 报告具体事项。原型验证不计为插件 Increment 已达到 DoD。

### 明确不纳入

- 任意数量会话、跨机器、跨平台、自动启动或终止 Claude 会话。
- 广播、自动选目标、自动重试、送达回执、事务性投递或并发吞吐指标。
- 自动恢复端点或静默替换 / 迁移旧配对；必要的迁移或显式重建须保留证据并明确产品路径。
- PBI-03 的完整投递状态能力、PBI-07 的完整安装维护能力。

不承诺并发吞吐或全局顺序，并不免除两个目标相近时间回复时的明确处理规则，见 S04-11-5。

### PBI-11 验收标准（审阅稿）

以下编号用于逐项验收与证据关联。验收细节待研究后与 PO 明确，可行性由 Developer 评估。

1. **S04-11-1 配对共存**：在声明支持的 Windows 环境中，同一个 Codex 原始会话能显式建立并保留至少两个正在运行的 Claude 原始会话目标。连接 B 不拆除 A；状态入口能辨认各目标、项目上下文、原会话和连接状态。
2. **S04-11-2 目标确定**：新发送有确定的目标，实际对象与用户选择一致。目标名称不存在、有歧义、身份失效或已断开时，操作说明原因与可执行下一步，不猜测、不静默切换。名称只是可读选择依据，不能替代稳定的原会话身份。
3. **S04-11-3 回复归属**：回复和追问沿被回复消息的关联绑定原目标，不随最近一次发送、当前选择或候选排序改变。关联已明确时无需 PO 再手写目标 ID；关联失效或与请求目标冲突时，不能将正文投给其他会话。
4. **S04-11-4 真实交流**：分别与 A、B 完成业务请求、回复、追问和后续回应；A、B 各自在其原始会话收到对应正文，同一个 Codex 原始会话收到两条交流的回复。切到 B 往来后再次回复 A 的旧消息仍正确。完整正文与自动 messageId / pairId / conversationId / replyTo、目标身份、数据位置和时间窗一致，另一目标无对应误投事件。
5. **S04-11-5 来信重叠边界**：两个独立目标可能相近时间向 Codex 回复。Developer 必须定义并验证系统在接纳、等待或拒绝时的可观察行为；已接纳消息不静默覆盖、丢弃、串目标或重复注入，未接纳与送达未知不能混为成功。容量不足或失败时有明确状态和后续动作，不诱导在投递结果未知时盲目重发。最小验收包含真实的重叠来信尝试及相应关联核对，不只演示人工完全串行的两条往返。
6. **S04-11-6 单目标生命周期与隔离**：A 停止、重启、断开、重连或替换时，B 的配对、待收信息、历史证据和实际通信仍可用。操作对象与影响明确；A 的待收和在途消息按约定处置，旧回复不会进入新原会话，数据保留与失败状态可查。
7. **S04-11-7 单目标与接收兼容**：只配置一个目标时，1.0.0 已验证的标准入口、身份校验、原会话收信、空闲唤醒、工作边界及重复抑制不回退。对既有单配对数据提供明确的继续使用、迁移或重建路径，先说明支持范围并保留证据；未经实测的自动升级不作承诺。
8. **S04-11-8 产品化**：改动在 `bridge/` 与 `plugins/claudetocodex/bridge/` 保持一致；相关 skill、状态 / 连接 / 回复文档及回归同步。最终验证由安装后的可追溯候选执行，记录源码、安装内容和宿主环境。

### 共同验收要求

- 收信按接收方原始会话中的完整正文、自动 messageId / pairId / conversationId / replyTo、目标身份、数据目录和时间窗核对。fixture、宿主加载、实际执行与原会话收信分别判定，`submitted:true` 或单条 context-prepared 不等于收信。
- 同一轮真实使用分别记录技术 AC 与 PO DoD，PO 无需手写标记、ID 或环境变量。

## 第三部分：Developer 工作区

本部分由 Developer 自行维护。

- 开始或恢复工作前完整重读本文件；范围与验收疑问及时澄清，必要时先做短研究。
- 自主制定方案、工作项、估计与顺序，并记录进展和对应验收证据。
- 尽早验证多目标真实使用闭环，及时向 SM 提出障碍及跨 Sprint 依赖。

### 工作项与进展

- 2026-09-13 开工。SM 经协调目录 s04-coordination-01a09641（任务消息 a44f48b5）传达 PO 已确认路径：Sprint 04 先做 PBI-11，以已发布 1.0.0 为基线，独立于 Sprint 03（03 的呈现改版与完整快捷连接管理留后续）。Developer 首轮回报经 codex queue 发送（消息 01a096a1），含范围/边界理解、worktree 方案、首批工作项与估计。
- 施工路径（SM 指示）：① 独立工作区短预研与原型 → ② 专用测试 Codex + Claude A/B 真实闭环 → ③ 原型成立后整合产品代码并在独立测试安装验收候选插件。
- 隔离边界（执行中）：原型阶段不改 `bridge/` 产品源码与 `plugins/claudetocodex/bridge/` 分发副本；不动日常已安装插件、缓存、marketplace、共享配置与在用默认桥数据（默认桥属其他项目且有待收，不改配对、不归档）；测试用专用 `CTC_BRIDGE_DIR` 隔离代码/配置/工作目录/桥数据/生效 hooks；本开发会话不作可随意重启的测试对端；宿主授权不绕过。
- worktree 与归属：`.claude/worktrees/s04-pbi-11`，分支 `worktree-s04-pbi-11`；已 merge 本地 main 的 2b46e77 与 6fd8866（SM 范围提交，无冲突，第三部分保留）。Developer 只写本文件第三部分与本冲刺目录 `prototype/` 内文件，按显式文件清单 stage，不整文件覆盖、不碰 SM 区域与 Product Backlog。
- 预研结论摘要：1.0.0 的单配对假设集中在 `pair.json` 单文件、`caller()` 唯一配对解析、`pending/<codexId>` 单待收槽、`handleHook`/`take` 单配对门控、`connect`/`send`/`status` 无多目标概念；`pairId`/`replyTo` 消息关联、`endpoints/` 按 sessionId 存放、唤醒文本携带 pairId、全局 `receipts/` 按 messageId 去重已具备多配对基础。详细触点清单与设计方案见 `prototype/DESIGN.md`。

| 工作项 | 内容 | 估计 | 状态 |
|---|---|---|---|
| W1 | 基线复读 + Developer 工作区填写 | 0.25d | 2026-09-13 完成 |
| W2 | 预研：多配对数据模型与路由设计笔记（`prototype/DESIGN.md`） | 0.5d | 2026-09-13 完成（含 SM 澄清后的 D4/D5 修订） |
| W3 | 原型骨架：隔离副本扩展多配对 + fixture 级测试（共存、回复归属、重叠三态、唤醒去重） | 1d | 2026-09-13 完成：`prototype/bridge/`（store/cli 改多配对，entry/sessions/delivery 沿用 1.0.0）+ `prototype/test/multi.test.mjs` 10/10 通过 |
| W4 | 真实闭环 harness：专用测试 Codex + Claude A/B（隔离桥数据），原会话收信与空闲唤醒验证 | 1d | 2026-09-13 完成：run3 线程 01a096d0 与 A=90cdb961/B=438a61f5 完成五场景（发送往返、干净自动往返、切 B 回 A 旧消息、重叠同待收、B 追问闭环），hook 真实执行、空闲唤醒两侧验证、SM 独立核入站帧；run1/run2 会话重启素材归档留证 |
| W5 | 首个检查点自查并向 SM 报告 | 0.25d | 2026-09-13 完成：四项判据（共存/切后归属/重叠三态/空闲唤醒）全过，AC 覆盖表与证据摘要见下节；报告经 codex queue 发送 |

检查点后另排：单目标失效隔离（S04-11-6）、单目标兼容与旧数据路径（S04-11-7）、产品树整合与安装候选验收（S04-11-8）。

### run3 真实闭环证据摘要（2026-09-13，Codex CLI 0.154.0 隔离 home + Claude Code 2.1.268）

证据规则（SM 82ad48d7）：以接收方原始入站帧/事件为主，模型复述与 received-log 仅辅助；SM 已独立核验 34bc8e32、d65bd878、7bea55c6、956f593d、c0bd3823、854500dc 的完整帧/正文/关联字段及接收会话正确性。

| AC | 状态 | 关键证据 |
|---|---|---|
| S04-11-1 配对共存 | 通过（真实环境） | run3 两配对并存（25784c0a→A、b2bd54db→B），connect B 不拆 A；status 可辨各目标与端点 |
| S04-11-2 目标确定 | 通过（真实） | A×3、B×3+ 均投正确原会话（入站帧核对）；同名歧义真实层两次如实拒绝并列候选；失效端点如实 send-error；未知目标/未知回复分支见 W6b 记录 |
| S04-11-3 回复归属 | 通过（真实） | c0bd3823 对 A 旧消息 reply：to=A（90cdb961）、conversationId 保持首轮 fa4bd398；当时最近往来为 B |
| S04-11-4 真实交流 | 通过（真实，B 追问闭环已补） | A 两轮、B 一轮+追问，自动唤醒回链（wake-submitted→context-prepared；明确不含 d65bd878 人工恢复样本）；1f8a1145 无入站（无误投） |
| S04-11-5 重叠来信 | 通过（真实） | 实际时间记录：65f7cb76 发布 18:31:42.255、上下文准备 18:31:51.701；bbe290fc 发布 18:31:46.183、上下文准备 18:32:02.185；两条 wake（44.352/46.701）均早于任一上下文准备；A 经 UserPromptSubmit、B 经 PostToolUse 逐事件进入，无覆盖无串目标；B 重复 wake 被抑制各一次；1f8a1145 同槽拒绝=未接纳。无 claim/槽快照，不声称并存时长下界。d65bd878 为人工恢复样本，另册不计自动唤醒 |
| S04-11-6 单目标生命周期隔离 | 通过（真实） | 停 A 后 B 全链路自动回链（6ea2c2f1）；旧端点直发与旧消息回复均如实 send-error 无误投（a6fd3f8a/7b13c457）；A 重启 connect 成新配对（1cec6785）、同名歧义如实拒绝；显式 retire（25784c0a）为重建边界、证据存 pairs-retired；恢复后新 A 自动回链（e9aced4d，自报 sessionId 前四位）。同名替换的名称歧义与退役入口已按真实场景验证处置（DESIGN §6 缺口的产品化方案归 PBI-10/13 精化） |
| S04-11-7 单目标兼容 | 通过（真实，除跨根拓扑限制外） | 单目标免 --name 真实往返不回退（77b5fb5d→d7392a3a）；legacy pair.json 只读可见、同身份沿用不新写、免 --name 真实送达、回复按约等待（de5319b9）；跨根 hooks 不投递为测试拓扑限制（单根即正常），如实在案。旧 pair.json 为按已验证 1.0.0 格式合成（PO 在用旧数据不耦合进测试） |
| S04-11-8 产品化 | 未开始 | 原型阶段刻意不动 `bridge/` 与 `plugins/` 产品树 |

过程中发现并修复（均限测试范围代码）：hooks.json BOM、项目受信条目、启动身份变量泄漏、回复指引缺 CODEX_HOME；详见 DESIGN §6。协作副产物：A/B 各提出一条真实产品建议（简化 reply 指引的风险与可溯源推断）。

### 检查点后工作分解（2026-09-13，SM 9624d5fd 授权继续）

| 工作项 | 内容 | 估计 | 状态 |
|---|---|---|---|
| W6 | 真实单目标生命周期隔离（S04-11-6）：停 A 验 B 全程可用；旧配对发送→端点失效如实 send-error 不误投；A 重启 connect 成新配对；同名歧义如实报错；补最小显式 retire 操作作为重建边界（仅原型）；旧消息回复不进新原会话 | 0.75d | 2026-09-13 主体完成：W6-1 停 A（B 自动回链 6ea2c2f1；旧端点发送 a6fd3f8a 与旧消息回复 7b13c457 均如实 send-error、无误投）；W6-2 新 A（10e98182）connect 成新配对 1cec6785、同名歧义两次如实拒绝、retire 25784c0a 后 send/reply 自动回链（ed46f072→e9aced4d，新 A 自报 sessionId 前四位）。途中修 retire --pairId 旗标 bug（84a74cb，SM 已独立复跑 16/16→17/17）；并发边界：pairs() 对 retire 移动中的文件按 ENOENT 跳过（本轮提交），retire 拒绝有待收未消费的目标 |
| W6b | CLI 异常分支真实层（补 S04-11-2）：未知目标/歧义目标/失效回复目标，可观察报错与下一步 | 0.25d | 2026-09-13 完成：同名歧义（W6 两次）与失效端点（a6fd3f8a/7b13c457）已有真实层证据；未知目标/未知回复目标分支已下发实测，结果追加 lifecycle-log |
| W7 | 单配对真实回归 + 旧数据路径（S04-11-7）：单目标完整往返不回退；legacy pair.json 只读识别、同身份沿用、跨身份共存的真实验证 | 0.5d | 2026-09-13 完成：单目标免 --name 真实往返（77b5fb5d→B 自动回链 d7392a3a，恢复 A 后 13cd259d）；legacy 根（1.0.0 格式合成、端点指向 B 真实端点）：可见、同身份 connect 沿用不新写（pairs/ 空）、免 --name 发送真实送达（19:06:49 pipe-written）、B 回复落 legacy 槽等待（de5319b9，19:07:08 published+wake-submitted）——跨根不投递为已记录测试拓扑限制（单根使用时 hooks 即服务该根） |
| W8 | 产品化（S04-11-8）：多配对改动整合进 `bridge/` 与 `plugins/claudetocodex/bridge/` 双树、skill/文档/回归同步、独立测试安装候选验收（PO 另定日常安装更新） | 1.5d | 未开始 |

未覆盖保留未测（SM 口径）：工作边界（收信打断当前模型调用的行为）、宿主异常注入形态等未实测项不因通过场景提前记通过。

### 障碍与待明确

- 协调通道：按 SM 指示用 codex queue 回报，不用标准桥 reply（避免在协调目录留无法投递的待收槽）；不改 SM hooks 与日常配对。
- 环境分工（SM 消息 cb7917e4 / f3ab48b8，PO 已同意）：Developer 准备并启动专用测试 Codex 与 Claude A/B，SM 核对隔离与证据；PO 触点仅限实际登录/信任/体验，届时给已备好的入口。
- ~~待 PO 决定：测试 Codex 登录方式~~ 已按 SM e1f9350c 撤回：认证内嵌于 provider 表（`requires_openai_auth=false` + bearer token），隔离 home 下 `codex doctor` auth ok、`codex exec` 真实调用通过——无需 PO 登录；后续若实际认证失败再报具体原因。整份复制 config 的做法已废弃，改为 `Copy-ModelConfig.ps1` 结构化提取（仅模型/provider 参数 + provider 表；日常 hooks/插件/marketplace/MCP/项目/信任态排除；凭据不打印、不进 Git）。
- 唯一保留的预期 PO 触点：测试窗口内一次性 hook 信任确认。`--dangerously-bypass-hook-trust` 旗标存在但按"不绕过授权"边界刻意不用。`approval_policy`/`sandbox_mode` 按用户日常值带入测试配置（保持会话行为一致），已向 SM 披露可复核。
- 宿主版本按 SM 只读核对：Codex CLI 0.154.0、Claude Code 2.1.268；相应宿主行为在 W4 实测，不沿用旧版本假设。
- 按 SM c9a1f0d9 口径：读取配置、创建测试文件不算宿主隔离已实证；隔离实证以真启动后的 `codex doctor --json` 加载核查与实际运行为准。
