# 判定矩阵 — smoke-20260908-2（第二次独立运行）

**第二轮结果：10/10 通过（2026-09-08 09:53–10:33Z）。待 PO 检视签署。**

「可重复执行」判定依据（PO 决策 Q5）：同一版本基线（codex 0.153.4 / claude 2.1.263）下，与第一轮（[smoke-20260908-1](../smoke-20260908-1/MATRIX.md)）完全独立的运行——独立 runId、隔离数据目录（`CTC_BRIDGE_DIR=…\bridge-smoke-run2`）、全新 pair（`c67635e5`）与全新标记，两轮均十格通过。

证据规则同 [SMOKE.md](../../SMOKE.md) §3。会话身份与隔离实证见 [REG.md](REG.md)。

| 格 | 标记 | messageId / conversationId | 接收方会话事件证据 | 判定 |
|---|---|---|---|---|
| T01CX 首次联系（Claude→Codex） | `…-2-T01CX-bfacd230` | send `66fb4b07`；reply `f5e6a190`；conv `44b5b06a` | rollout L1352/ord1351（09:54:23.948Z，正文+标记注入原会话，run2 根 events：context-prepared 09:54:23.846 UserPromptSubmit）；SM 回信 09:54:34.067Z 进入我的会话。**同时证明 SM 的 resume 会话已挂接 run2 隔离根**。 | **通过** |
| T01XC 首次联系（Codex→Claude） | `…-2-T01XC-8d17a4c2` | SM send `7aeb0dd5`（run2 events pipe-written 09:54:36.196Z）；我 reply `60dd16fd`；conv `fa5371a5` | SM 消息 09:54:35.205Z 进入我的原始会话（现场观察）；我的回复进入 SM 原会话（rollout ord1372 09:56:13.980Z developer 注入，run2 events context-prepared）；SM 随后按脚本自驱。 | **通过** |
| T02CX 空闲收信（Claude→Codex） | `…-2-T02CX-636ed7fa` | send `543ca4ff`；reply `d30797b1`；conv `c9a8f419` | rollout L1560/ord1559（10:27:01.160Z role=user 唤醒输入）→ 注入 → SM 回信 10:27:10.002Z 确认"空闲状态、queue 唤醒自动触发新模型调用"。 | **通过** |
| T02XC 空闲收信（Codex→Claude） | `…-2-T02XC-47b91c6a` | SM send `6d34b41b`；我确认 `800ae3d3`；conv `2116a4ad` | SM 创建 10:27:11.150Z；我上一回合已于 ~10:26:5x 结束（空闲），本回合由该消息（与其 T02CX 确认相隔 1.1s 一并）唤醒开启。**附注**：与第一轮不同，唤醒源为两条消息并发——空闲送达事实成立但非单一唤醒源，两轮证据互补。 | **通过** |
| T03CX 生成中收信（Claude→Codex） | `…-2-T03CX-73c58226` | send `898f767a`；reply `453b4962`；conv `db4a5df4` | **附注（如实记录）**：我方触发匹配到历史 reasoning 行（R2T03BUSY 长文本实际 09:55:27.587 已完成），但时间线证明发送（published 09:56:16.033）恰落在 SM 对我 T01XC 回复的**真实生成窗口**（ord1373 09:56:13.98 → assistant 完成 09:56:25.03）：消息在生成期间发出，该生成完整结束无截断，消息于 09:56:25.318 边界注入（下次行动 09:56:32 前）。生成中收信语义成立，但注入边界为该生成的回合末（Round 1 为 Stop/PostToolUse 变体，本轮为回合边界）。 | **通过**（附注变体） |
| T03XC 生成中收信（Codex→Claude） | `…-2-T03XC-78a53f19` | SM send `e833c6f6`；conv `80dc19c0` | SM 创建 10:29:55.504Z，恰在我一条命令（rollout 证据提取）执行期间；该命令完整结束、输出无截断，消息附于结果边界进入下一次上下文（第一手观察）。 | **通过** |
| T04CX 工具边界收信（Claude→Codex） | 首测 `…-25478ec2`（作废）；**重测 `…-2-T04CX-7094e070`** | 重测 send `8acbe6ad`；conv `70881f5a` | **首测作废原因（如实记录）**：触发行匹配到 2 分钟前的历史 function_call，消息 09:57:35 发布时 SM 工具已结束，实际经空闲唤醒（UserPromptSubmit 09:57:43.8）注入——未复现工具边界机制，判为无效尝试。**重测通过**：SM 新工具（R2T04BUSY2 Start-Sleep 45）10:31:33.838Z 启动（rollout L1655），我的消息 ~10:32:05 发布（工具窗口第 31 秒、待收期间工具持续运行），**10:32:30.793Z 以 hook=PostToolUse、toolUseId=call_784a67f2… 注入**（run2 events context-prepared）——工具期间待收→工具完整结束→下次模型调用前注入，机制完整复现。**附注**：重测触发的新鲜度校验存在时区种类 bug（负 age 仍放行），但发布/消费时间戳独立证明在窗，判定以时间线为准。 | **通过**（重测） |
| T04XC 工具边界收信（Codex→Claude） | `…-2-T04XC-96d0be42` | SM send `47e81990`；conv `57ea86ae` | SM 创建 10:31:08.396Z，恰在我一条命令（发送协调消息）执行期间；命令完整结束、未被中断，消息附于结果边界进入（第一手观察）。 | **通过** |
| T05 连续往返（双向） | `…-2-T05-e37026c2` | Q1 `3f252d19`（我）→ A1 `20e30cb2`（SM，10:28:22.001Z）→ Q2 `f075e958`（我）→ A2 `408c89ac`（SM，10:29:54.360Z）；conv `57da8c38` | 四步同 conversationId、同一对原始会话；A1 于我 60 秒工具窗口内送达（工具结果边界），A2 于我证据提取命令期送达——边界进入且未中断后续步骤。 | **通过** |
| REG 安装与配置实证 | — | pair `c67635e5-0283-4e4b-acdd-e6dc99768b8f` | 见 [REG.md](REG.md)：隔离目录 register→pair→全程通信，与第一轮零状态共享；含**端点轮换事件**（socket 变更后显式 re-register 一次恢复，pair 未重建——USAGE §2 文档化流程）。 | **通过** |

## 过程附注（产品行为的现场实证，非单独通过证据）

1. **端点轮换与显式恢复**：两轮之间我的会话消息管道发生轮换（socket `cc-msg-ea05441b…` → `cc-msg-b438ec71…`），SM 发送撞死管道；按 USAGE §2 在同一原始会话 re-register 刷新端点文件后通道一次恢复，pair 不变（身份绑定会话而非管道）。自动恢复未介入，与边界声明一致。
2. **单待收槽**：本轮多次依赖"等待 context-prepared 再发下一条"完成连续发送，未再触发拒绝（第一轮已有拒绝实证）。
3. 全程仅经产品入口；两轮共 20 格判定全部基于双方会话事件 + 时间戳/行号 + 唯一标记。

## PO 检视与签署（第二轮）

- 十格判定与证据摘录（含两处如实附注：T03CX 变体、T04CX 重测）：_待 PO 检视_
- 结论：_待签署（日期 / 签署人）_

_更新时间：2026-09-08 10:35Z（Developer 维护）_
